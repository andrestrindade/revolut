How to run: Paste create.sql and init.sql into your home folder, and run as Java Application.
It will be acessable throught the adress http://localhost:4567/transferMoney/accountFrom/ValueToTransfer/accountTo.
There are two accounts in init.sql, it's created when the connection starts.